name: build-portable-binaries

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
    tags:
      - "v*"
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            asset_name: mdvi-linux-x86_64-musl.tar.gz
            binary_suffix: ""
            package_stem: mdvi-linux-x86_64-musl
            build_with_zig: true
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            asset_name: mdvi-linux-aarch64-musl.tar.gz
            binary_suffix: ""
            package_stem: mdvi-linux-aarch64-musl
            build_with_zig: true
          - os: macos-15-intel
            target: x86_64-apple-darwin
            asset_name: mdvi-macos-x86_64.tar.gz
            binary_suffix: ""
            package_stem: mdvi-macos-x86_64
            build_with_zig: false
          - os: macos-14
            target: aarch64-apple-darwin
            asset_name: mdvi-macos-aarch64.tar.gz
            binary_suffix: ""
            package_stem: mdvi-macos-aarch64
            build_with_zig: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            asset_name: mdvi-windows-x86_64.zip
            binary_suffix: ".exe"
            package_stem: mdvi-windows-x86_64
            build_with_zig: false

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Restore Rust build cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install Zig
        if: matrix.build_with_zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.13.0

      - name: Install cargo-zigbuild
        if: matrix.build_with_zig
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-zigbuild

      - name: Build release binary with Zig
        if: matrix.build_with_zig
        run: cargo zigbuild --locked --release --target ${{ matrix.target }}

      - name: Build release binary
        if: ${{ !matrix.build_with_zig }}
        run: cargo build --locked --release --target ${{ matrix.target }}

      - name: Package Unix artifact
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          package_dir="dist/${{ matrix.package_stem }}"
          mkdir -p "$package_dir"
          cp "target/${{ matrix.target }}/release/mdvi${{ matrix.binary_suffix }}" "$package_dir/"
          cp README.md LICENSE "$package_dir/"
          tar -C dist -czf "dist/${{ matrix.asset_name }}" "${{ matrix.package_stem }}"
          rm -rf "$package_dir"

      - name: Package Windows artifact
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $packageDir = "dist/${{ matrix.package_stem }}"
          New-Item -ItemType Directory -Force $packageDir | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/mdvi${{ matrix.binary_suffix }}" $packageDir
          Copy-Item README.md $packageDir
          Copy-Item LICENSE $packageDir
          Compress-Archive -Path "$packageDir/*" -DestinationPath "dist/${{ matrix.asset_name }}"
          Remove-Item -Recurse -Force $packageDir

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.package_stem }}
          path: dist/${{ matrix.asset_name }}

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download packaged artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"

          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$tag" release-assets/* --clobber --repo "$GITHUB_REPOSITORY"
          else
            gh release create "$tag" release-assets/* --generate-notes --repo "$GITHUB_REPOSITORY"
          fi
